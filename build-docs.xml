<?xml version="1.0" encoding="UTF-8"?>
<project name="sierra_help_manual" basedir="." default="all">
	<import file="./definitions.xml" />

	<target name="all" depends="design-intent, sierra-guide, flashlight-guide" />

	
	
	<target name="design-intent" depends="design-intent-html, design-intent-eclipse-help, design-intent-pdf" />	

	<target name="design-intent-html">
		<docbook-to-html
			  srcdir=  "${di.source.dir}"
		    srcfile= "${di.source.file}"
			  destdir= "${di.html.dir}"
			  destfile="${di.html.file}"/>
	</target>

	<target name="design-intent-eclipse-help">
		<docbook-to-eclipse-help
		    srcdir=  "${di.source.dir}"
		    srcfile= "${di.source.file}"
		    destdir= "${di.eclipse.help.dir}"/>
	</target>
		
	<target name="design-intent-pdf">
		<docbook-to-pdf srcfile="${di.source.file}"
			  destfile="${di.pdf.file}"
	      destdir= "${di.build.dir}"/>
  </target>
	
  	
  	
	<target name="sierra-guide" depends="sierra-guide-html, sierra-guide-eclipse-help, sierra-guide-pdf" />

	<target name="sierra-guide-html">
		<docbook-to-html
			  srcdir=  "${sg.source.dir}"
		    srcfile= "${sg.source.file}"
			  destdir= "${sg.html.dir}"
			  destfile="${sg.html.file}"/>
	</target>

	<target name="sierra-guide-eclipse-help">
		<docbook-to-eclipse-help
		    srcdir=  "${sg.source.dir}"
		    srcfile= "${sg.source.file}"
		    destdir= "${sg.eclipse.help.dir}"/>
	</target>

	<target name="sierra-guide-pdf">
		<docbook-to-pdf srcfile="${sg.source.file}"
			  destfile="${sg.pdf.file}"
			  destdir= "${sg.build.dir}"/>
  </target>

  	
  	
	<target name="flashlight-guide" depends="flashlight-guide-html, flashlight-guide-eclipse-help, flashlight-guide-pdf" />

	<target name="flashlight-guide-html">
		<docbook-to-html
			  srcdir=  "${fg.source.dir}"
		    srcfile= "${fg.source.file}"
			  destdir= "${fg.html.dir}"
			  destfile="${fg.html.file}"/>
	</target>

	<target name="flashlight-guide-eclipse-help">
		<docbook-to-eclipse-help
		    srcdir=  "${fg.source.dir}"
		    srcfile= "${fg.source.file}"
	  	  destdir= "${fg.eclipse.help.dir}"/>
	</target>

	<target name="flashlight-guide-pdf">
		<docbook-to-pdf srcfile="${fg.source.file}"
			  destfile="${fg.pdf.file}"
        destdir= "${fg.build.dir}"/>
  </target>



	<target name="clean">
		<delete quiet="true" dir="${build}" />
	</target>
</project>